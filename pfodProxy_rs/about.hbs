{{!--
  Handlebars template for cargo-about.  Renders the dep tree into the
  attribution markdown file shipped alongside the pfodProxy binary.
  Handlebars's built-in helpers are minimal — no length / counting —
  so the template just emits "License — crates that use it — text"
  per license without trying to be clever.
--}}# Third-Party Licenses

This binary statically links the following open-source crates.  Their
copyright notices and license texts are reproduced here per their
respective licenses.

The pfodProxy source itself is © Forward Computing and Control Pty. Ltd.
and is distributed under the project's own terms (see `LICENSE`).

---

{{#each licenses}}
## {{name}}

Used by:

{{#each used_by}}
- **{{crate.name}}** {{crate.version}}{{#if crate.repository}} — <{{crate.repository}}>{{/if}}
{{/each}}

```
{{text}}
```

---

{{/each}}
