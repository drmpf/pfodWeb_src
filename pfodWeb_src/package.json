{
  "name": "pfodweb-build",
  "version": "1.0.0",
  "description": "Build tools for pfodWeb standalone HTML bundle",
  "private": true,
  "scripts": {
    "build": "node build-bundle.js"
  },
  "devDependencies": {
    "terser": "^5"
  }
}
