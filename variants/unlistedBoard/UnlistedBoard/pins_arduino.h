// pins_arduino.h stub for the "Unlisted Board" Designer target.
//
// This target generates standard Arduino code (no specific board pin map).
// The file exists only so build_boards.js's variant discovery (which looks
// for pins_arduino.h) finds this variant; buildCcodeBoard() in build_boards.js
// does not parse this file's contents and always emits an empty pins[] array.
