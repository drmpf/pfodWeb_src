/*
  pins_arduino.h - Pin definition functions for Arduino
  Part of Arduino - http://www.arduino.cc/

  LilyPad Arduino: same ATmega328P/168 pin map as the standard Uno-family
  boards, run at 3.3 V / 8 MHz instead of 5 V / 16 MHz (see this board's
  own board.json for the resulting ADC reference-voltage difference).
*/

#include "../standard/pins_arduino.h"
