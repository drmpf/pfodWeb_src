/*
  pins_arduino.h - Pin definition functions for Arduino

  Arduino Industrial 101: same ATmega32U4 pin map as the Yún family (this
  is a real, unmodified copy of the same Leonardo-derived silicon) — the
  difference on this specific board is entirely which pins are
  physically wired to a usable header, not a different chip. See this
  board's own board.json for the confirmed 7-pin exposed subset.
*/

#include "../leonardo/pins_arduino.h"
