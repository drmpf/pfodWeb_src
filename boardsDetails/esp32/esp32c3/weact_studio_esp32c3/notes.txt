Board: weact_studio_esp32c3 (WeAct Studio ESP32-C3 Core Board, no board-specific board.json)
Date: 2026-07-14 (documentation backfill — findings from an earlier audit pass, not new research)

Sources consulted:
- None board-specific. This board has no board.json of its own anywhere
  under variants/esp32/esp32c3/weact_studio_esp32c3/ (only a
  pins_arduino.h is present). No ancestor board.json exists between it
  and the chip-level default either — the chip-level default at
  variants/esp32/esp32c3/board.json is the only board.json that applies
  to this board. That file's only cited source is "Espressif ESP-IDF
  documentation (docs.espressif.com)" (see that file's own chipGpios
  comment).

Per-pin findings:
- No board-specific tagging exists for this board. build_boards.js's
  ancestor-fallback mechanism serves this board the plain chip-level
  default board.json unchanged. That default's chipGpios entries are the
  generic ESP32-C3 pin set (GPIO0-11, 20-21), each carrying only generic
  capabilities (analog_input/digital_input/digital_output/pwm_output as
  applicable per pin, per the Espressif ESP-IDF chip documentation) — no
  button, LED, NeoPixel, or exclusion findings of any kind. GPIOs 12-17
  (flash) and GPIOs 18-19 (USB) are absent from chipGpios chip-wide (not
  board-specific). ESP32-C3 has no hardware DAC and every exposed GPIO is
  bidirectional (inputOnlyGpios: []), per the chip-level board.json's own
  comments.

Summary: This board has no board-specific board.json — it uses the shared
chip-level default at variants/esp32/esp32c3/board.json unchanged,
identical to sibling boards that also fall back to the same ancestor
(e.g. AirM2M_CORE_ESP32C3, XIAO_ESP32C3, codecell, m5stack_stamp_c3,
nologo_esp32c3_super_mini, wifiduinov2). No board-specific button/LED/
exclusion research has been done for this board; none was found in its
directory.
