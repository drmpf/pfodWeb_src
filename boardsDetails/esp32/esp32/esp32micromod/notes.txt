Board: esp32micromod (chip: esp32)
Date: 2026-07-14 (documentation backfill — findings from an earlier audit pass, not new research)

Sources consulted:
- vendor pins_arduino.h (this board's own pins_arduino.h file, cited
  in-line in board.json's header comment as the source for the I2C
  interrupt line and onboard audio codec I2S bus exclusions below).
- SparkFun MicroMod ESP32 Processor Board Hookup Guide: "this pin is
  connected to the status LED (active high)".

Per-pin findings:
- GPIO4: excluded (capabilities: [] - absent from chipGpios). Note from
  board.json: I2C interrupt line (see vendor pins_arduino.h).
- GPIO16, GPIO17: excluded (capabilities: [] - absent from chipGpios).
  Note from board.json: dedicated onboard audio codec I2S bus (see
  vendor pins_arduino.h). (Already excluded chip-wide by default for
  PSRAM use; also independently listed here for the audio codec I2S bus.)
- GPIO25, GPIO26: excluded (capabilities: [] - absent from chipGpios).
  Note from board.json: dedicated onboard audio codec I2S bus (see
  vendor pins_arduino.h).
- GPIO2: tagged "led_high" (capabilities: ["digital_output", "led_high",
  "pwm_output"]). Note from board.json: confirmed onboard STAT LED,
  active-high (SparkFun MicroMod ESP32 Processor Board Hookup Guide:
  "this pin is connected to the status LED (active high)").
- All remaining chipGpios entries (0,1,3,5,12,13,14,15,18,19,21,22,23,27,
  32,33,34,35,36,37,38,39) are unchanged from the esp32/esp32 chip-level
  default (variants/esp32/esp32/board.json) - plain analog/digital/pwm/dac
  capabilities per pin, no additional board-specific findings.

Summary: GPIO4 excluded as the I2C interrupt line, and GPIO16/17/25/26
excluded as the dedicated onboard audio codec I2S bus, per this board's
own vendor pins_arduino.h; GPIO2 tagged led_high as the onboard STAT LED
per the SparkFun MicroMod ESP32 Processor Board Hookup Guide; all other
pins left at the chip-level esp32/esp32 default.
