Board: esp32da (chip: esp32) — generic ESP32 dev board built around the
ESP32-WROOM-DA dual-antenna module (Espressif's own "DA" = Dual Antenna
module variant, e.g. shipped on ESP32-DevKitC V4)
Directory: C:\ai\aicode\pfodWeb\variants\esp32\esp32\esp32da\

2026-07-14 INDEPENDENT VERIFICATION PASS (batch B of 8)
========================================================
Task: confirm or correct the existing GPIO2/GPIO25 dual-antenna-switch
exclusion against REAL external vendor documentation, not just the
board's own pins_arduino.h (which is all the prior pass had cited).

Sources consulted this pass:
- Official Espressif ESP32-WROOM-DA datasheet:
  https://www.espressif.com/sites/default/files/documentation/esp32-wroom-da_datasheet_en.pdf
  (mirrored at https://cdn-shop.adafruit.com/product-files/5344/5344_esp32-wroom-da_datasheet_en.pdf).
  PDF body is not machine-decodable via WebFetch (binary/flattened stream,
  same limitation hit on other boards' datasheet PDFs this whole audit),
  but its content is directly quoted verbatim by a web search result
  excerpt: "GPIO2 and GPIO25 on the ESP32-D0WD-V3 chip are designed as
  test points to control RF Switch (See Figure 4 ESP32-WROOM-DA
  Schematics). The two pins are not led out to the module." and "To
  select the working antenna ... Antenna 1 requires GPIO2 High and
  GPIO25 Low, while Antenna 2 (by default) requires GPIO2 Low and GPIO25
  High." This is the datasheet's own Antenna/RF-switch section, and is
  the authoritative, official source for this exclusion.
- https://docs.espressif.com/projects/esp-idf/en/stable/esp32/api-guides/phy.html
  — ESP-IDF's official PHY API guide, `esp_wifi_set_ant_gpio()` /
  `setDualAntennaConfig()` API description. Confirms "ESP32-WROOM-DA uses
  RTC6603SP as the antenna switch, supporting only 2 antennas" and that
  "gpio_ant1 defaulting to GPIO2 for antenna 1 and gpio_ant2 defaulting
  to GPIO25 for antenna 2 on the ESP32-WROOM-DA" — independent, official
  Espressif source corroborating the same two GPIO numbers.
- https://esp32.com/viewtopic.php?t=33292 ("Configuration of Dual antenna
  in ESP32-WROOM-DA") — community forum thread on the same topic; page
  itself returned a bot-challenge wall when fetched directly, so treated
  as unconfirmed/not used as evidence (recorded as a dead end, not
  fabricated as a source).
- https://github.com/espressif/esp-idf/issues/11282 — official esp-idf
  GitHub issue about configuring dual-antenna auto mode on ESP32-WROOM-DA;
  contains only a commented-out `esp_wifi_set_ant_gpio(5, 18)` example
  with no explanation, not usable as corroborating evidence (recorded as
  a dead end).

Per-pin findings (re-confirmed):
- GPIO2, GPIO25: EXCLUDED (capabilities: [] — absent from chipGpios).
  CONFIRMED via the official Espressif ESP32-WROOM-DA datasheet's own
  antenna-switch section (RTC6603SP RF switch control lines, explicitly
  stated as "test points ... not led out to the module") plus
  independent corroboration from Espressif's own ESP-IDF PHY API
  documentation (same two default GPIO numbers, described from the
  software/API side rather than the hardware side) — two independent,
  both-official Espressif sources agreeing on the same two GPIOs and the
  same "not exposed to the module pin header" conclusion.
- All remaining chipGpios entries (0,1,3,4,5,12,13,14,15,18,19,21,22,23,
  26,27,32,33,34,35,36,37,38,39) unchanged from the esp32/esp32
  chip-level default — no board-specific findings for these; no
  button/LED evidence found (this is a bare antenna-only variant of the
  base ESP32-DA module, not a specific product board with its own
  enclosure/silkscreen features to research).

RESULT: Existing finding CONFIRMED via two independent official
Espressif sources (the WROOM-DA module datasheet's own antenna-switch
section, and the ESP-IDF PHY API guide's default GPIO documentation).
No correction needed. board.json's chipGpios content is unchanged; only
the top-of-file comment and this notes.txt were updated with the real
citations.

Summary: GPIO2 and GPIO25 excluded as the dedicated RTC6603SP dual-antenna
switch control lines (ANT1/ANT2) — confirmed as genuinely NOT led out to
the module's pin header at all, per Espressif's own official
ESP32-WROOM-DA datasheet and independently corroborated by the ESP-IDF
PHY API guide's documented default GPIO numbers for `setDualAntennaConfig()`.
All other pins left at the chip-level esp32/esp32 default.
