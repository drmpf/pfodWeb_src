Board: espectro32 (chip: esp32) - DycodeX ESPectro32
Date: 2026-07-14 (documentation backfill — findings from an earlier audit pass, not new research)

Sources consulted:
- github.com/dycodex/ESPectro32, src/ESPectro32_Constants.h and
  src/ESPectro32_LED.cpp turnOn() -> digitalWrite(pin, HIGH) (DycodeX's
  official ESPectro32 library).
- github.com/dycodex/ESPectro32, src/ESPectro32_Constants.h,
  src/ESPectro32_RGBLED.h (class ESPectro32_RGBLED : public WS2812).
- github.com/dycodex/ESPectro32, src/ESPectro32_Constants.h,
  ESPectro32_Board.h (Button A / Button B definitions).

Per-pin findings:
- GPIO33: excluded (capabilities: [] - absent from chipGpios). Note from
  board.json: dedicated onboard microSD card chip-select (SD_SS), not
  general-purpose.
- GPIO15: tagged "led_high" (capabilities: ["digital_output", "led_high",
  "pwm_output"]). Note from board.json: confirmed onboard single-color
  LED (ESPECTRO32_LED_PIN) in DycodeX's official ESPectro32 library,
  active-high by default (github.com/dycodex/ESPectro32,
  src/ESPectro32_Constants.h and src/ESPectro32_LED.cpp turnOn() ->
  digitalWrite(pin, HIGH)). Matches LED_BUILTIN.
- GPIO27: tagged "led_neopixel" (capabilities: ["digital_output",
  "led_neopixel", "pwm_output"]). Note from board.json: confirmed onboard
  WS2812/Neopixel RGB LED (ESPECTRO32_RGBLED_PIN, class
  ESPectro32_RGBLED : public WS2812) in DycodeX's official library
  (github.com/dycodex/ESPectro32, src/ESPectro32_Constants.h,
  src/ESPectro32_RGBLED.h).
- GPIO0: tagged "button" (capabilities: ["digital_input", "button"]).
  Note from board.json: confirmed onboard Button A
  (ESPECTRO32_BUTTON_A_PIN) in DycodeX's official ESPectro32 library
  (github.com/dycodex/ESPectro32, src/ESPectro32_Constants.h,
  ESPectro32_Board.h).
- GPIO4: tagged "button" (capabilities: ["digital_input", "button"]).
  Note from board.json: confirmed onboard Button B
  (ESPECTRO32_BUTTON_B_PIN) in DycodeX's official ESPectro32 library
  (github.com/dycodex/ESPectro32, src/ESPectro32_Constants.h,
  ESPectro32_Board.h).
- All remaining chipGpios entries (1,2,3,5,12,13,14,18,19,21,22,23,25,26,
  32,34,35,36,37,38,39) are unchanged from the esp32/esp32 chip-level
  default (variants/esp32/esp32/board.json) - plain analog/digital/pwm/dac
  capabilities per pin, no additional board-specific findings. (Note:
  GPIO33 does not appear as its own capabilities entry since it is fully
  excluded above.)

Summary: GPIO33 excluded as the dedicated onboard microSD card
chip-select; GPIO15 tagged led_high as the onboard single-color LED;
GPIO27 tagged led_neopixel as the onboard WS2812 RGB LED; GPIO0 and
GPIO4 tagged button as onboard Button A and Button B respectively - all
confirmed via DycodeX's official ESPectro32 library source on GitHub;
all other pins left at the chip-level esp32/esp32 default.
