Board: esp32thing (chip: esp32) - SparkFun ESP32 Thing
Date: 2026-07-14 (documentation backfill — findings from an earlier audit pass, not new research)

Sources consulted:
- SparkFun ESP32 Thing Hookup Guide: Blink example uses GPIO 5,
  digitalWrite(ledPin, HIGH) turns it on.
- SparkFun ESP32 Thing Hookup Guide: WiFi example reads "button 0" on
  GPIO 0, active-low.

Per-pin findings:
- GPIO5: tagged "led_high" (capabilities: ["digital_output", "pwm_output",
  "led_high"]). Note from board.json: confirmed active-high onboard LED
  (SparkFun ESP32 Thing Hookup Guide: Blink example uses GPIO 5,
  digitalWrite(ledPin, HIGH) turns it on).
- GPIO0: tagged "button" (capabilities: ["digital_input", "button"]).
  Note from board.json: confirmed onboard user button (SparkFun ESP32
  Thing Hookup Guide: WiFi example reads "button 0" on GPIO 0,
  active-low).
- All remaining chipGpios entries (1,2,3,4,12,13,14,15,18,19,21,22,23,25,
  26,27,32,33,34,35,36,37,38,39) are unchanged from the esp32/esp32
  chip-level default (variants/esp32/esp32/board.json) - plain
  analog/digital/pwm/dac capabilities per pin, no additional
  board-specific findings.

Summary: GPIO5 tagged led_high as the onboard active-high LED, and GPIO0
tagged button as the onboard active-low user button, both per the
SparkFun ESP32 Thing Hookup Guide; all other pins left at the chip-level
esp32/esp32 default.
