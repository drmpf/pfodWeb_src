Board: ttgo-t7-v14-mini32 (TTGO T7 V1.4 Mini32, ESP32, no board-specific board.json)
Date: 2026-07-14 (documentation backfill — findings from an earlier audit pass, not new research)

Sources consulted:
- None board-specific. This board has no board.json of its own anywhere
  under variants/esp32/esp32/ttgo-t7-v14-mini32/ (only a pins_arduino.h is
  present). No ancestor board.json exists between it and the chip-level
  default either — the chip-level default at variants/esp32/esp32/board.json
  is the only board.json that applies to this board. That file's only
  cited source is "Espressif ESP-IDF documentation (docs.espressif.com)"
  (see that file's own chipGpios comment).

Per-pin findings:
- No board-specific tagging exists for this board. build_boards.js's
  ancestor-fallback mechanism serves this board the plain chip-level
  default board.json unchanged. That default's chipGpios entries are the
  generic ESP32 pin set (GPIO0-5, 12-15, 18-19, 21-23, 25-27, 32-39), each
  carrying only generic capabilities (analog_input_serial/digital_input/
  digital_output/pwm_output/dac_output as applicable per pin, per the
  Espressif ESP-IDF chip documentation) — no button, LED, NeoPixel, or
  exclusion findings of any kind. GPIOs 6-11 (flash) and 16-17 (PSRAM) are
  absent from chipGpios chip-wide (not board-specific); GPIOs 34-39 are
  input-only.

Summary: This board has no board-specific board.json — it uses the shared
chip-level default at variants/esp32/esp32/board.json unchanged, identical
to sibling boards that also fall back to the same ancestor (e.g.
ttgo-t7-v13-mini32, quantum, xinabox, BharatPi-A7672S-4G). No
board-specific button/LED/exclusion research has been done for this
board; none was found in its directory.
