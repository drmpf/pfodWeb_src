Board: gpy (Pycom GPy), chip esp32
Date: 2026-07-14 (documentation backfill - findings from an earlier audit pass, not new research)

Sources consulted:
- Vendor pins_arduino.h for this board (PIN_RGB_LED=0 "->2812 RGB" alias).
- Pycom firmware source: MICROPY_HW_HB_PIN_NUM=0 (heartbeat pin definition) and
  modled.c (GRB/RMT WS2812 driver), cited in board.json's own inline comment
  as the confirming source for GPIO0's RGB LED function.
(No other external URLs were cited in this board's board.json comments;
the two references above - vendor pins_arduino.h and the Pycom firmware
source files - are the full set already documented at the top of the file.)

Per-pin findings:
- GPIO0: tagged ["digital_output", "pwm_output", "led_neopixel"]. Confirmed
  onboard WS2812 RGB LED - Pycom firmware's MICROPY_HW_HB_PIN_NUM=0 heartbeat
  pin definition and modled.c's GRB/RMT driver, matching vendor pins_arduino.h's
  PIN_RGB_LED=0 ("->2812 RGB") alias.
- GPIO1, 2, 3, 4, 12, 13, 14, 15, 22, 25, 26, 32, 33, 34, 35, 36, 37, 38, 39:
  left at the plain chip-level default capability set (no board-specific
  note; standard ESP32 digital/analog/pwm/dac capabilities per chip family).
- GPIO5, 18, 19, 23, 27: excluded (removed from chipGpios entirely) - dedicated
  onboard Sequans LTE modem UART/control lines, per vendor pins_arduino.h.
- GPIO21: excluded (removed from chipGpios entirely) - dedicated onboard WiFi
  antenna switch, per vendor pins_arduino.h.

Summary: GPIO0 tagged led_neopixel (onboard WS2812 RGB LED, confirmed via Pycom
firmware + vendor pins_arduino.h); GPIO5/18/19/21/23/27 excluded as dedicated
LTE-modem and antenna-switch lines; all other pins left at plain ESP32 chip
defaults.
