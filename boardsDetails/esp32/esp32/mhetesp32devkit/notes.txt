Board: mhetesp32devkit (MH-ET Live ESP32 DevKit), chip esp32
Date: 2026-07-14 (documentation backfill - findings from an earlier audit pass, not new research)

This board has NO board.json of its own anywhere under
variants/esp32/esp32/mhetesp32devkit/ - only a pins_arduino.h file exists
in that directory. Per build_boards.js's ancestor-fallback mechanism, this
board therefore uses the plain chip-level default
variants/esp32/esp32/board.json unmodified (no vendor-specific pin tags or
exclusions were ever added for this board specifically).

Sources consulted:
- None specific to this board - no board-specific board.json or inline
  pfodWeb NOTE comments exist to cite. The chip-level esp32/esp32/board.json
  this board falls back to cites only its own generic source: Espressif
  ESP-IDF documentation (docs.espressif.com), used to justify the generic
  ESP32 chipGpios set (GPIO6-11 flash-reserved, GPIO16-17 PSRAM-reserved,
  GPIO34-39 input-only, GPIO20/24/28-31 nonexistent in silicon).

Per-pin findings:
- All chipGpios entries (GPIO0-5, 12-15, 18-19, 21-23, 25-27, 32-39) match
  the plain chip-level esp32/esp32/board.json default exactly: standard
  digital_input/digital_output/pwm_output (+ analog_input/analog_input_serial/
  dac_output where applicable per chip capability), with GPIO34-39 input-only
  and GPIO6-11/16-17/20/24/28-31 absent chip-wide. No button, LED, or
  dedicated-hardware tags/exclusions specific to this board were found or
  applied - this board was never individually audited.

Summary: mhetesp32devkit has no board-specific board.json; it shares the
identical plain chip-level esp32/esp32/board.json default with several
sibling boards (e.g. mhetesp32minikit, lolin32) that also rely on the same
ancestor-fallback mechanism. No board-specific findings exist to document.
