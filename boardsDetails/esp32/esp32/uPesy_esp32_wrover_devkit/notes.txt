Board: uPesy_esp32_wrover_devkit
Directory: C:\ai\aicode\pfodWeb\variants\esp32\esp32\uPesy_esp32_wrover_devkit
pins_arduino.h: LED_BUILTIN = GPIO2 (alias only), TX = GPIO1, RX = GPIO3.
No board.json existed for this variant prior to this research pass; none was created,
since no capability tag was applied (see below).

Sources consulted:
- https://www.upesy.com/blogs/tutorials/upesy-esp32-wrover-devkit-board-documentation-version-latest
  Vendor's own WROVER board doc page. No "Details on pin numbers" table listing
  LED_BUILTIN is present here (unlike the sibling WROOM devkit's doc page, which
  does have that table). Only LED mentioned anywhere on the page is: "if the
  integrated red LED does not light up when the board is powered up, there is a
  power supply problem" (Built-in protections / Usage recommendations section) -
  this reads as a hardwired power-on indicator LED, not a GPIO-controlled/software
  user LED, and no GPIO number is given for it. No push button other than
  generic EN/BOOT is mentioned.
- https://www.upesy.com/products/upesy-esp32-wrover-devkit-board
  Product page. No mention of any onboard user LED or push button GPIO.
- https://www.upesy.com/blogs/tutorials/getting-started-with-upesy-esp32-wrover-devkit-board
  Getting-started tutorial. Covers PSRAM enablement/pinout/programming only; no
  LED or button GPIO mentioned.
- https://docs.platformio.org/en/latest/boards/espressif32/upesy_wrover.html
  PlatformIO board doc. Hardware specs only (mcu/freq/flash/ram/vendor); no
  LED/button GPIO info.
- https://www.espboards.dev/esp32/upesy-wrover/
  Third-party aggregator. Shows a pin table row "Pin 2 - A12 - T2 - PWM -
  LED_BUILTIN" - but this label is clearly just reproducing the Arduino core's
  pins_arduino.h LED_BUILTIN alias (same generic pattern as most ESP32 dev-kit
  cores), not an independent vendor schematic confirmation. Per task rules this
  does NOT count as solid confirming evidence on its own.
- https://www.upesy.com/blogs/tutorials/upesy-esp32-wroom-low-power-devkit-board-documentation-version-latest
  Checked for a possible cross-reference. Its "no blue LED on GPIO2" comparison
  text explicitly contrasts only against "the uPesy Wroom DevKit board" - it
  does NOT mention or compare to the Wrover DevKit, so it neither confirms nor
  denies a Wrover LED.
- WebSearch "uPesy ESP32 Wrover DevKit push button OR reset button OR boot
  button GPIO" - results confirm the board only has the standard EN (reset) and
  BOOT (flash-mode strapping) buttons, both dedicated control buttons, not a
  general-purpose user-input GPIO button. Consistent with the sibling WROOM
  devkit finding from the earlier audit pass ("no onboard user push button").
- learn.upesy.com/en/boards/ESP32_Wrover_DevKit.html - attempted fetch, domain
  did not resolve (DNS failure), could not be consulted.

Pins considered but NOT tagged:
- GPIO2 (LED_BUILTIN alias / candidate LED pin): NOT TAGGED. This is a
  deliberate no-guess decision, not a judgment call in favor of tagging. The
  vendor's WROVER-specific documentation does not contain the pin-numbering
  table that confirmed the sibling WROOM board's blue LED, and the only LED
  the vendor page describes is a non-GPIO power-indicator LED. The espboards.dev
  "LED_BUILTIN" table entry is pins_arduino.h-derived, which the task
  explicitly disallows as sole evidence. A human reviewer with access to the
  actual uPesy Wrover schematic/KiCad files (not found via web search) could
  confirm or deny whether GPIO2 also drives a real onboard LED on this variant,
  the same as on the WROOM devkit.
- EN / BOOT buttons: NOT TAGGED (no guess made). Confirmed to be dedicated
  reset/flash-strapping buttons, not general-purpose user-input buttons, so
  they are correctly out of scope for the "button" capability tag, matching
  the WROOM sibling's finding.

Summary: NO CHANGE. No confirmable onboard user-controllable LED or user
push button was found for uPesy_esp32_wrover_devkit via vendor documentation;
no board.json was created or modified.
