Board: doitESPduino32 (chip: esp32) - DOIT ESPduino32
Date: 2026-07-14 (documentation backfill — findings from an earlier audit pass, not new research)

Sources consulted:
- espboards.dev and circuitstate.com pinout references for the DOIT ESP32
  DEVKIT V1 / WeMos D1 R32 clone reference design this board is based on,
  plus common blink examples, confirming digitalWrite(HIGH) turns the
  onboard LED on.

Per-pin findings:
- GPIO2: tagged "led_high" (capabilities: ["digital_output", "led_high", "pwm_output"]).
  Note from board.json: confirmed onboard LED_BUILTIN, active-high (DOIT
  ESP32 DEVKIT V1 / WeMos D1 R32 clone reference designs this board is
  based on; espboards.dev and circuitstate.com pinout references + common
  blink examples confirm digitalWrite(HIGH) turns the onboard LED on).
- All other chipGpios entries (0,1,3,4,5,12,13,14,15,18,19,21,22,23,25,
  26,27,32,33,34,35,36,37,38,39) are unchanged from the esp32/esp32
  chip-level default (variants/esp32/esp32/board.json) - plain
  analog/digital/pwm/dac capabilities per pin, no additional
  board-specific findings.

Summary: GPIO2 tagged led_high as the onboard active-high LED_BUILTIN,
based on DOIT ESP32 DEVKIT V1 reference-design pinout sources; all other
pins left at the chip-level esp32/esp32 default.
