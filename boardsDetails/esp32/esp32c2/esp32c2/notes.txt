Board: esp32c2 (ESP32-C2 Dev Module, no board-specific board.json)
Date: 2026-07-14 (documentation backfill — findings from an earlier audit pass, not new research)

Sources consulted:
- None board-specific. This board has no board.json of its own anywhere
  under variants/esp32/esp32c2/esp32c2/ (only a pins_arduino.h is
  present). The chip-level default at variants/esp32/esp32c2/board.json
  is the only board.json that applies to this board — and since this is
  the sole board variant currently defined under the esp32c2 chip bucket,
  there are no sibling boards sharing this fallback. That chip-level
  file's only cited source is "Espressif ESP-IDF documentation
  (docs.espressif.com)" (see that file's own chipGpios comment).

Per-pin findings:
- No board-specific tagging exists for this board. build_boards.js's
  ancestor-fallback mechanism serves this board the plain chip-level
  default board.json unchanged. That default's chipGpios entries are the
  generic ESP32-C2 pin set (GPIO0-10, 18-20), each carrying only generic
  capabilities (analog_input/digital_input/digital_output/pwm_output as
  applicable per pin, per the Espressif ESP-IDF chip documentation) — no
  button, LED, NeoPixel, or exclusion findings of any kind. GPIOs 11-17
  (flash) are absent from chipGpios chip-wide (not board-specific).
  ESP32-C2 has no hardware DAC and every exposed GPIO is bidirectional
  (inputOnlyGpios: []), per the chip-level board.json's own comments.

Summary: This board has no board-specific board.json — it uses the shared
chip-level default at variants/esp32/esp32c2/board.json unchanged. No
board-specific button/LED/exclusion research has been done for this
board; none was found in its directory.
