Board: Generic RP2350 (rp2350/generic) — catch-all template entry, not a real vendor product
Date: 2026-07-13

Note: the audit agent for this pair (generic + generic_rp2350) hit a
session-limit API error after writing both board.json files but before
writing either notes.txt. Completed manually from the verified board.json
content rather than re-running the agent.

Sources consulted:
- This board's own pins_arduino.h + shared ../generic/common.h — the
  30-pin RP2350A-compatible template (GPIO0-22, GPIO26-28), pin-identical
  to the official Raspberry Pi Pico 2 (rpipico2, same chip bucket).
- variants/rp2040/rp2350/rpipico2/board.json — the confirmed-via-official-
  datasheet precedent this generic entry borrows its GPIO25 LED convention
  from (PICO_DEFAULT_LED_PIN 25, active-high, no _INVERTED override).

Per-pin reasoning:
- GPIO25 (PIN_LED/LED_BUILTIN) — inherited from the shared template; no
  single real product exists behind this catch-all entry, so the
  rpipico2 convention is applied by direct analogy since the template is
  pin-identical. Tagged ["digital_output","pwm_output","led_high"].
- No button or NeoPixel alias declared in this pins_arduino.h — none to
  tag.
- No SPI/I2C alias collision on GPIO25 (SPI0/SPI1/Wire0/Wire1 pins are
  GPIO4-5,12-19,26-27).

Summary: Generic RP2350 — GPIO25 LED tagged by analogy to rpipico2 (same
shared template); explicitly documented as a template default, not
vendor-researched hardware, since a real DIY board may not match.
