Board: Arduino Mega / Mega 2560 (avr/mega)
Date: 2026-07-14

Sources consulted:
- docs.arduino.cc LED_BUILTIN language reference (official) — "The
  built-in LED of the Arduino board is connected internally to the
  'D13' pin".

Per-pin findings:
- D13 (LED_BUILTIN) — confirmed onboard status LED, active-high. Not on
  this board's SPI bus (SPI is D50-D53 on Mega), so no dual-role caveat
  needed. Tagged ["digital_output","pwm_output","led_high"].

megaADK (onboard MAX3421E USB host controller) was split off into its
own directory — see that board.json/notes.txt for its dedicated-hardware
exclusion findings; it shares this same confirmed D13 LED.

Summary: Mega/Mega 2560 (+ Mega ADK via the same D13 LED fact) — D13
onboard LED confirmed and tagged active-high.
